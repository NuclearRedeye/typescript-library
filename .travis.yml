sudo: false

language: node_js
node_js:
  - "lts/*"

install:
  - npm install

script:
  - npm run lint
  - npm run test

deploy:
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    branch: master
    tags: true